services:
  frontend:
    build: 
      context: ./Diary Project
    ports:
      - "80:5173"
    depends_on:
      - backend

  backend:
    build: ./chatbot
    network_mode: "host"
    ports:
      - "8000:8000"